apiVersion: v1
kind: PersistentVolume
metadata:
  name: fastpass-postgres-pv
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/fastpass/postgres
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fastpass-postgres-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  volumeName: fastpass-postgres-pv
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: fastpass-postgres-backup-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/fastpass/backups
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fastpass-postgres-backup-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: fastpass-postgres-backup-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastpass-postgres
  namespace: default
  labels:
    app: fastpass-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fastpass-postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: fastpass-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_DB
          value: fastpass
        - name: POSTGRES_USER
          value: fastpass
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: fastpass-postgres-secret
              key: postgres-password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        ports:
        - containerPort: 5432
          name: postgres
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        - name: postgres-backup
          mountPath: /backups
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - fastpass
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - fastpass
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: fastpass-postgres-pvc
      - name: postgres-backup
        persistentVolumeClaim:
          claimName: fastpass-postgres-backup-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: fastpass-postgres-service
  namespace: default
spec:
  selector:
    app: fastpass-postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP
