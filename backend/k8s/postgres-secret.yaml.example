apiVersion: v1
kind: Secret
metadata:
  name: fastpass-postgres-secret
  namespace: default
type: Opaque
stringData:
  # PostgreSQL password for fastpass database
  # CHANGE THIS before deploying!
  postgres-password: "CHANGE_ME_TO_SECURE_PASSWORD"
