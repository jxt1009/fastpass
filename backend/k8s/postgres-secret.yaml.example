apiVersion: v1
kind: Secret
metadata:
  name: fasttrack-postgres-secret
  namespace: default
type: Opaque
stringData:
  # PostgreSQL password for fasttrack database
  # CHANGE THIS before deploying!
  postgres-password: "CHANGE_ME_TO_SECURE_PASSWORD"
