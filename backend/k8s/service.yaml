apiVersion: v1
kind: Service
metadata:
  name: fastpass-api
  namespace: default
spec:
  selector:
    app: fastpass-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
