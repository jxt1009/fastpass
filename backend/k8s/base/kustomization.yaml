apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Image is set by each overlay's kustomization.yaml (production/staging).
# CI updates it at deploy time via: kustomize edit set image fasttrack-api=...
