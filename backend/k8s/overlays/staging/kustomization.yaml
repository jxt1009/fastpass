apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fasttrack-staging

resources:
  - ../../base
  - namespace.yaml

# Single replica is fine for staging
patches:
  - path: patch-deployment.yaml
  - path: patch-ingress.yaml

commonLabels:
  environment: staging
