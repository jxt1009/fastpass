# Legacy Website Assets

`website/` is deprecated.

Public pages are served by the backend (`backend/public_pages.go`), and standalone website deploys are intentionally disabled (`.github/workflows/website-deploy.yml`).

Keep these files only for historical reference while migration cleanup continues.
