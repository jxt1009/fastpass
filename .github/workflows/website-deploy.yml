name: Website Deploy

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Website deploy disabled
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: echo "Public pages are served by the backend deployment; standalone website deploys are disabled."
