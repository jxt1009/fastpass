// Secrets.swift.template — copy this to Secrets.swift and fill in real values.
// Secrets.swift is gitignored and must NEVER be committed.
//
// How to get these values:
//   Google Client ID: https://console.cloud.google.com → APIs & Services → Credentials
//   Create an OAuth 2.0 Client ID → iOS → Bundle ID: com.toper.FastTrack
enum Secrets {
    static let googleClientID = "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com"
}
